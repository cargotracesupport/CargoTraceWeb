# Capacitor shell — generated / installed artifacts
node_modules/
# The native Android project is generated by `npx cap add android`. Commit it
# once you start customizing it (signing, icons, manifest); until then it's
# reproducible from this scaffold, so keep it out of git.
/android/
